<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Web Arch Events</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-gray-50 min-h-screen">
    <%- include('../partials/header') %>
    
    <main class="container mx-auto px-4 py-8">
        <%- include('../partials/flash-messages') %>
        
        <div class="max-w-7xl mx-auto">
            <div class="text-center mb-12">
                <h1 class="text-4xl font-bold text-gray-900 mb-4">Welcome to Web Arch Events</h1>
                <p class="text-xl text-gray-600 mb-8">Discover and create amazing events in your community</p>
                <div class="space-x-4">
                    <a href="/events" class="bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700">Browse Events</a>
                </div>
            </div>

            <div class="mb-12">
                <h2 class="text-2xl font-bold text-gray-900 mb-6">Featured Events</h2>
                <% if (events && events.length > 0) { %>
                    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                        <% events.forEach(event => { %>
                            <div class="bg-white rounded-lg shadow-md overflow-hidden">
                                <div class="p-6">
                                    <h3 class="text-lg font-semibold text-gray-900 mb-2"><%= event.title %></h3>
                                    <p class="text-gray-600 mb-4"><%= event.description || 'No description available' %></p>
                                    <div class="flex items-center text-sm text-gray-500 mb-4">
                                        <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                                        </svg>
                                        <%= new Date(event.eventDate).toLocaleDateString() %>
                                        <% if (event.eventTime) { %>
                                            at <%= new Date(event.eventTime).toLocaleTimeString() %>
                                        <% } %>
                                    </div>
                                    <% if (event.location) { %>
                                        <div class="flex items-center text-sm text-gray-500 mb-4">
                                            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
                                            </svg>
                                            <%= event.location %>
                                        </div>
                                    <% } %>
                                    <a href="/events/<%= event.id %>" class="text-blue-600 hover:text-blue-800 font-medium">View Details</a>
                                </div>
                            </div>
                        <% }) %>
                    </div>
                <% } else { %>
                    <div class="text-center py-12">
                        <p class="text-gray-500 text-lg">No events available at the moment.</p>
                    </div>
                <% } %>
            </div>
        </div>
    </main>
    
    <%- include('../partials/footer') %>
    
    <script src="/js/main.js"></script>
</body>
</html>
